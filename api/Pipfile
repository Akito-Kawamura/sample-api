[[source]]
url = "https://pypi.org/simple"
verify_ssl = true
name = "pypi"

[packages]
fastapi = "*"
uvicorn = {extras = ["standard"], version = "*"}

[dev-packages]
black = "==22.3.0"

[requires]
python_version = "3.9"

[scripts]
setup = "uvicorn main:app --reload"
fmt = "black ."
